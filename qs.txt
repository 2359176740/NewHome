1、为什么refs取不到值

2、ref变为数组 当数据双向绑定的时候

   ref 有些情况下是数组，有些情况下是对象？？？？？查资料
   同样都是绑定，在组建绑定就是数组，拿出来就是一个对象，为什么？？？

3、watch监控高度变化，this.nexttrict并不能实现一个数据渲染完再渲染另一个 settimout 可以


4、如何使用vue修改关键字，title标签内容

5、数据分流时注意事项


网站缺点：点击分类之后，需要重新回到最初的选项，最好拆分的细一点，划分不同的路由，这样用户体验好
公共方法封装成一个函数